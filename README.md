# Hone — Quant Arithmetic

Mental arithmetic trainer.

## Deploy

Vercel is the intended host (TanStack Start / Nitro). Guest play works without a database; sign-in sync needs Postgres (`DATABASE_URL`) — see `PUBLISH.md`.
